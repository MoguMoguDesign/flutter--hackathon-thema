import 'package:flutter_test/flutter_test.dart';
// import 'package:flutterhackthema/features/tcg/tournament/presentation/providers/tournament_provider.dart';
// import 'package:hooks_riverpod/hooks_riverpod.dart';

/// トーナメントプロバイダーのテスト
///
/// Note: build_runner実行後にコメントを外してテストを有効化
void main() {
  group('TournamentsProvider', () {
    test('初期状態は空のリスト', () {
      // TODO: build_runner実行後に実装
      // final container = ProviderContainer();
      // addTearDown(container.dispose);
      //
      // final tournaments = container.read(tournamentsProvider);
      // expect(tournaments, isEmpty);
    });

    test('トーナメントを追加できる', () {
      // TODO: build_runner実行後に実装
      // final container = ProviderContainer();
      // addTearDown(container.dispose);
      //
      // container.read(tournamentsProvider.notifier).addTournament('Test Tournament');
      // final tournaments = container.read(tournamentsProvider);
      // expect(tournaments, contains('Test Tournament'));
    });
  });

  group('SelectedTournamentProvider', () {
    test('初期状態はnull', () {
      // TODO: build_runner実行後に実装
      // final container = ProviderContainer();
      // addTearDown(container.dispose);
      //
      // final selected = container.read(selectedTournamentProvider);
      // expect(selected, isNull);
    });
  });
}
