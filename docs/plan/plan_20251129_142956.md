# 実装プラン: Issue #9 - ルーティング設定 (go_router 統合)

**作成日:** 2025-11-29
**プランナー:** Claude Code
**対象Issue:** #9 - Routing Configuration (go_router Integration)
**フェーズ:** Phase 1 - Foundation Building
**優先度:** High (Red)
**ブランチ:** `feature/routing-configuration`

---

## 目次

1. [エグゼクティブサマリー](#エグゼクティブサマリー)
2. [背景と目的](#背景と目的)
3. [現状分析](#現状分析)
4. [実装戦略](#実装戦略)
5. [詳細タスク分解](#詳細タスク分解)
6. [ファイル変更計画](#ファイル変更計画)
7. [テスト戦略](#テスト戦略)
8. [リスク分析と対策](#リスク分析と対策)
9. [技術スタック互換性](#技術スタック互換性)
10. [実装チェックリスト](#実装チェックリスト)

---

## エグゼクティブサマリー

### 概要

Flutter Hackathon Thema プロジェクトのグローバルルーティング設定を go_router 16.x を使用して実装します。本実装により、アプリケーション全体の画面遷移とナビゲーションの基盤を構築します。

### 主要な変更点

- **main.dart の変更**: MaterialApp → MaterialApp.router
- **ルート定義の追加**: /nickname、/posts、/create、/preview の4つのルート
- **リダイレクトガードの実装**: ニックネーム未設定時の自動リダイレクト
- **エラーページの強化**: 既存のエラービルダーを強化
- **テスト追加**: ルーティングの包括的なテストカバレッジ

### 実装規模

- **新規作成ファイル**: 9ファイル（コード6 + テスト3）
- **変更ファイル**: 3ファイル
- **削除ファイル**: 0ファイル
- **推定工数**: 2-3時間

### 依存関係

- **前提条件**: Issue #8（プロジェクト構造組織化）の完了
- **ブロック対象**: Issue #10, #12, #13, #15（各画面実装）

---

## 背景と目的

### プロジェクト背景

本プロジェクトは、俳句とAI生成画像を組み合わせた投稿共有Webアプリケーションです。ユーザーは以下の流れで利用します：

1. ニックネーム入力（初回のみ）
2. 俳句入力
3. DALL-E 3による画像生成
4. プレビュー確認と投稿
5. 全ユーザーの投稿一覧表示

### Issue #9 の目的

- **グローバルルーティングの確立**: アプリ全体の画面遷移の基盤構築
- **型安全なナビゲーション**: go_router による宣言的ルーティング
- **認証ガードの実装**: ニックネーム未設定ユーザーの自動リダイレクト
- **エラーハンドリング**: 404エラーやルーティングエラーの適切な処理

### 三層アーキテクチャとの整合性

```
App Layer (アプリ層)
  ├─ app_router/         ← このIssueで実装
  │   ├─ app_router.dart
  │   ├─ routes.dart
  │   └─ (将来的に go_router_builder でコード生成)
  └─ route_guard/        ← このIssueで実装
      └─ nickname_guard.dart

Feature Layer (機能層)
  ├─ nickname/           ← Issue #10 で実装
  ├─ posts/              ← Issue #12 で実装
  └─ post_creation/      ← Issue #13, #15 で実装

Shared Layer (共有層)
  └─ presentation/
      └─ pages/
          └─ error_page.dart  ← このIssueで実装
```

**依存関係ルールの遵守:**
- ✅ App → Feature (ルートから各画面への参照)
- ✅ App → Shared (エラーページの使用)
- ❌ Feature → Feature (禁止 - 遵守)

---

## 現状分析

### 既存実装の確認

#### 1. main.dart (現状)

```dart
import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

void main() {
  runApp(const ProviderScope(child: MainApp()));
}

class MainApp extends StatelessWidget {
  const MainApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: 'Flutter App', home: const Scaffold());
  }
}
```

**問題点:**
- MaterialApp を使用（MaterialApp.router ではない）
- ルーティング設定が未使用
- ProviderScope は正しく配置されている ✅

#### 2. lib/app/app_router/app_router.dart (現状)

```dart
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

class AppRouter {
  static GoRouter createRouter() {
    return GoRouter(
      initialLocation: '/',
      routes: [
        GoRoute(
          path: '/',
          name: 'home',
          builder: (context, state) => const Scaffold(),
        ),
      ],
      errorBuilder: (context, state) => Scaffold(/* エラー表示 */),
    );
  }
}
```

**評価:**
- ✅ 基本構造は良好
- ✅ エラービルダーが実装されている
- ❌ ルート定義が不完全（/ のみ）
- ❌ リダイレクトガードが未実装

#### 3. lib/app/app_router/routes.dart (現状)

```dart
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

class HomeRoute extends GoRouteData {
  const HomeRoute();

  @override
  Widget build(BuildContext context, GoRouterState state) {
    return const Scaffold();
  }
}

class Routes {
  static const String home = '/';
}
```

**評価:**
- ✅ 型安全ルート定義の準備がある
- ❌ go_router_builder によるコード生成が未使用
- ❌ 必要なルートが定義されていない

### 技術的制約

1. **Issue #8 への依存**
   - lib/features/ ディレクトリがまだ作成されていない
   - 各画面（NicknamePage, PostsPage等）が未実装
   - **対策**: プレースホルダーページを一時的に lib/app/widgets/pages/ に作成

2. **go_router_builder の使用**
   - 現在は未使用だが、将来的に採用予定
   - 型安全性の強化のため、現時点では基本的な GoRoute で実装
   - Issue #8 完了後に go_router_builder へ移行可能

3. **ニックネーム管理**
   - Issue #10（ニックネーム機能）未実装のため、一時的な provider を作成
   - shared_preferences は Issue #10 で追加されるため、現時点ではメモリ上で管理

---

## 実装戦略

### アプローチ

#### フェーズ1: 基本ルーティング構造の構築

1. **main.dart の変更**
   - MaterialApp → MaterialApp.router
   - AppRouter.createRouter() の統合

2. **ルート定義の追加**
   - /nickname - ニックネーム入力画面
   - /posts - 投稿一覧画面
   - /create - 投稿作成画面
   - /preview - プレビュー画面

3. **プレースホルダーページの作成**
   - lib/app/widgets/pages/ に一時的なページを配置
   - Issue #8 完了後に lib/features/ へ移行

#### フェーズ2: リダイレクトガードの実装

1. **NicknameGuard の作成**
   - ニックネーム未設定時に /nickname へリダイレクト
   - /nickname 自体はリダイレクト対象外

2. **一時的な Provider の作成**
   - lib/app/app_di/nickname_provider.dart
   - メモリ上でニックネームを管理
   - Issue #10 完了時に shared_preferences 対応版に置き換え

#### フェーズ3: エラーページの強化

1. **専用エラーページの作成**
   - lib/shared/presentation/pages/error_page.dart
   - 404エラーの詳細表示
   - ホームへの戻るボタン

#### フェーズ4: テストの実装

1. **ルーティングテスト**
   - 各ルートへの遷移テスト
   - リダイレクトガードの動作確認
   - エラーページの表示テスト

### 設計原則

1. **依存関係の最小化**
   - Issue #8 への依存を最小限に抑える
   - プレースホルダーページで代替

2. **移行の容易性**
   - Issue #8 完了後の移行を考慮した設計
   - ページの配置変更のみで対応可能

3. **三層アーキテクチャの遵守**
   - App Layer: ルーティング設定
   - Shared Layer: エラーページ
   - Feature Layer: 各画面（Issue #8 以降）

---

## 詳細タスク分解

### タスク1: main.dart の変更

**優先度:** High
**推定工数:** 15分

**作業内容:**
1. MaterialApp を MaterialApp.router に変更
2. routerConfig に AppRouter.createRouter() を設定
3. 不要な home パラメータを削除

**成功基準:**
- アプリが正常に起動すること
- ルーターが正しく初期化されること

---

### タスク2: ルート定義の追加

**優先度:** High
**推定工数:** 30分

**作業内容:**
1. routes.dart にルート定数を追加
   - Routes.nickname = '/nickname'
   - Routes.posts = '/posts'
   - Routes.create = '/create'
   - Routes.preview = '/preview'

2. app_router.dart に GoRoute を追加
   - 各ルートのビルダーを定義
   - プレースホルダーページを一時的に使用

**成功基準:**
- 各ルートへのナビゲーションが機能すること
- context.go('/posts') で画面遷移できること

---

### タスク3: プレースホルダーページの作成

**優先度:** High
**推定工数:** 30分

**作業内容:**
1. lib/app/widgets/pages/ ディレクトリを作成
2. 以下のプレースホルダーページを作成:
   - placeholder_nickname_page.dart
   - placeholder_posts_page.dart
   - placeholder_create_post_page.dart
   - placeholder_preview_page.dart

**ページ仕様:**
- タイトル表示
- 「このページは準備中です」メッセージ
- ナビゲーションボタン（他のページへ遷移可能）

**成功基準:**
- 各ページが正常に表示されること
- ページ間の遷移が動作すること

---

### タスク4: ニックネームProviderの作成

**優先度:** High
**推定工数:** 20分

**作業内容:**
1. lib/app/app_di/nickname_provider.dart を作成
2. @riverpod アノテーションで StateNotifier を実装
3. メモリ上でニックネームを管理

**Provider仕様:**
```dart
@riverpod
class TemporaryNickname extends _$TemporaryNickname {
  @override
  String? build() => null;

  void setNickname(String nickname) {
    state = nickname;
  }

  void clearNickname() {
    state = null;
  }
}
```

**成功基準:**
- ニックネームの設定・取得が機能すること
- コード生成が正常に動作すること

---

### タスク5: リダイレクトガードの実装

**優先度:** High
**推定工数:** 30分

**作業内容:**
1. lib/app/route_guard/ ディレクトリを作成
2. nickname_guard.dart を作成
3. GoRouter の redirect パラメータでガードを適用

**ガードロジック:**
```dart
String? nicknameGuard(BuildContext context, GoRouterState state, WidgetRef ref) {
  final nickname = ref.read(temporaryNicknameProvider);
  final isNicknamePage = state.matchedLocation == Routes.nickname;

  // ニックネームが未設定 かつ ニックネームページ以外の場合
  if (nickname == null && !isNicknamePage) {
    return Routes.nickname;
  }

  return null; // リダイレクトなし
}
```

**成功基準:**
- ニックネーム未設定時に /nickname へリダイレクトされること
- ニックネーム設定後は他のページへアクセスできること

---

### タスク6: エラーページの作成

**優先度:** Medium
**推定工数:** 20分

**作業内容:**
1. lib/shared/presentation/pages/error_page.dart を作成
2. 既存の errorBuilder を ErrorPage に置き換え

**ページ仕様:**
- エラーアイコン表示
- エラーメッセージ（URLパス表示）
- ホームへ戻るボタン
- スタイルは lib/src/constants/ の定数を使用

**成功基準:**
- 存在しないルートにアクセスした際にエラーページが表示されること
- ホームへ戻るボタンが機能すること

---

### タスク7: コード生成の実行

**優先度:** High
**推定工数:** 5分

**作業内容:**
```bash
fvm flutter pub run build_runner build --delete-conflicting-outputs
```

**成功基準:**
- nickname_provider.g.dart が生成されること
- ビルドエラーが発生しないこと

---

### タスク8: 静的解析とフォーマット

**優先度:** High
**推定工数:** 10分

**作業内容:**
```bash
# フォーマット
dart format --set-exit-if-changed .

# 静的解析
fvm flutter analyze
```

**成功基準:**
- すべてのファイルが正しくフォーマットされていること
- 静的解析エラーが0件であること

---

### タスク9: テストの実装

**優先度:** High
**推定工数:** 45分

**作業内容:**
1. test/app/app_router/app_router_test.dart
   - ルート定義のテスト
   - 初期ルートのテスト
   - エラービルダーのテスト

2. test/app/route_guard/nickname_guard_test.dart
   - ニックネーム未設定時のリダイレクトテスト
   - ニックネーム設定済み時のテスト
   - ニックネームページ自体のテスト

3. test/shared/presentation/pages/error_page_test.dart
   - エラーページの表示テスト
   - ホームボタンの動作テスト

**テストカバレッジ目標:**
- ユニットテスト: 80%以上
- Widgetテスト: 60%以上

**成功基準:**
- すべてのテストがパスすること
- テストカバレッジが目標値を達成すること

---

## ファイル変更計画

### 新規作成ファイル (9ファイル)

#### App Layer - Routing Configuration

1. **lib/app/route_guard/nickname_guard.dart**
   - 役割: ニックネーム認証ガード
   - サイズ: 約50行
   - 依存: app_di/nickname_provider.dart

#### App Layer - Dependency Injection

2. **lib/app/app_di/nickname_provider.dart**
   - 役割: 一時的なニックネーム管理 Provider
   - サイズ: 約30行
   - 依存: riverpod_annotation

3. **lib/app/app_di/nickname_provider.g.dart**
   - 役割: 自動生成ファイル
   - 生成コマンド: build_runner

#### App Layer - Placeholder Pages

4. **lib/app/widgets/pages/placeholder_nickname_page.dart**
   - 役割: ニックネーム入力画面のプレースホルダー
   - サイズ: 約80行
   - 依存: shared/constants

5. **lib/app/widgets/pages/placeholder_posts_page.dart**
   - 役割: 投稿一覧画面のプレースホルダー
   - サイズ: 約80行
   - 依存: shared/constants

6. **lib/app/widgets/pages/placeholder_create_post_page.dart**
   - 役割: 投稿作成画面のプレースホルダー
   - サイズ: 約80行
   - 依存: shared/constants

7. **lib/app/widgets/pages/placeholder_preview_page.dart**
   - 役割: プレビュー画面のプレースホルダー
   - サイズ: 約80行
   - 依存: shared/constants

#### Shared Layer - Error Handling

8. **lib/shared/presentation/pages/error_page.dart**
   - 役割: 404エラーページ
   - サイズ: 約100行
   - 依存: shared/constants

#### App Layer - Directory Creation

9. **lib/app/widgets/pages/.gitkeep**
   - 役割: ディレクトリ保持用（一時的）
   - サイズ: 0行

---

### 変更ファイル (3ファイル)

#### 1. lib/main.dart

**変更内容:**
```dart
// 変更前
return MaterialApp(title: 'Flutter App', home: const Scaffold());

// 変更後
return MaterialApp.router(
  title: 'Flutter Hackathon Thema',
  routerConfig: AppRouter.createRouter(),
);
```

**変更行数:** 約5行
**影響範囲:** アプリ全体のルーティング

---

#### 2. lib/app/app_router/app_router.dart

**変更内容:**
1. ルート定義の追加（4つのルート）
2. redirect パラメータの追加
3. errorBuilder の ErrorPage への置き換え
4. initialLocation の変更（/ → /nickname）

**変更行数:** 約60行
**影響範囲:** アプリ全体のルーティング

**変更後の構造:**
```dart
class AppRouter {
  static GoRouter createRouter(WidgetRef ref) {
    return GoRouter(
      initialLocation: Routes.nickname,
      routes: [
        GoRoute(path: Routes.nickname, ...),
        GoRoute(path: Routes.posts, ...),
        GoRoute(path: Routes.create, ...),
        GoRoute(path: Routes.preview, ...),
      ],
      redirect: (context, state) => nicknameGuard(context, state, ref),
      errorBuilder: (context, state) => ErrorPage(error: state.error),
    );
  }
}
```

---

#### 3. lib/app/app_router/routes.dart

**変更内容:**
1. Routes クラスに定数追加
2. HomeRoute の削除（不要）
3. ドキュメントコメントの追加

**変更行数:** 約15行
**影響範囲:** ルート定義の参照箇所

**変更後:**
```dart
class Routes {
  /// ニックネーム入力画面のパス
  static const String nickname = '/nickname';

  /// 投稿一覧画面のパス
  static const String posts = '/posts';

  /// 投稿作成画面のパス
  static const String create = '/create';

  /// プレビュー画面のパス
  static const String preview = '/preview';
}
```

---

### 削除ファイル (0ファイル)

なし

---

### テストファイル (3ファイル)

#### 1. test/app/app_router/app_router_test.dart

**テスト内容:**
- ルート定義の存在確認
- 初期ルートのテスト
- 各ルートへのナビゲーションテスト
- エラービルダーのテスト

**テストケース数:** 約10ケース

---

#### 2. test/app/route_guard/nickname_guard_test.dart

**テスト内容:**
- ニックネーム未設定時のリダイレクト
- ニックネーム設定済み時の通常動作
- /nickname 自体はリダイレクト対象外

**テストケース数:** 約8ケース

---

#### 3. test/shared/presentation/pages/error_page_test.dart

**テスト内容:**
- エラーページの表示テスト
- エラーメッセージの表示確認
- ホームボタンの動作確認

**テストケース数:** 約5ケース

---

### ディレクトリ構造（変更後）

```
lib/
├── main.dart                                      # 変更
├── app/
│   ├── app_di/
│   │   ├── nickname_provider.dart                 # 新規
│   │   └── nickname_provider.g.dart               # 生成
│   ├── app_router/
│   │   ├── app_router.dart                        # 変更
│   │   └── routes.dart                            # 変更
│   ├── route_guard/
│   │   └── nickname_guard.dart                    # 新規
│   └── widgets/
│       └── pages/
│           ├── placeholder_nickname_page.dart     # 新規
│           ├── placeholder_posts_page.dart        # 新規
│           ├── placeholder_create_post_page.dart  # 新規
│           └── placeholder_preview_page.dart      # 新規
└── shared/
    └── presentation/
        └── pages/
            └── error_page.dart                    # 新規

test/
├── app/
│   ├── app_router/
│   │   └── app_router_test.dart                   # 新規
│   └── route_guard/
│       └── nickname_guard_test.dart               # 新規
└── shared/
    └── presentation/
        └── pages/
            └── error_page_test.dart               # 新規
```

---

## テスト戦略

### テストカバレッジ目標

| テストタイプ | 目標カバレッジ | 優先度 |
|------------|------------|--------|
| ユニットテスト | 80%以上 | High |
| Widgetテスト | 60%以上 | High |
| 統合テスト | - | Medium（将来実装） |

### テスト1: ルーティングテスト

**ファイル:** test/app/app_router/app_router_test.dart

**テストケース:**

```dart
group('AppRouter', () {
  testWidgets('初期ルートは /nickname であること', (tester) async {
    // Given: ルーターを作成
    final router = AppRouter.createRouter(ref);

    // When: アプリを起動
    await tester.pumpWidget(MaterialApp.router(routerConfig: router));

    // Then: /nickname が表示されること
    expect(router.routeInformationProvider.value.uri.path, equals('/nickname'));
  });

  testWidgets('/posts へのナビゲーションが機能すること', (tester) async {
    // Given: ニックネームが設定されている状態
    ref.read(temporaryNicknameProvider.notifier).setNickname('テストユーザー');

    // When: /posts へ遷移
    router.go('/posts');
    await tester.pumpAndSettle();

    // Then: PostsPage が表示されること
    expect(find.text('投稿一覧（準備中）'), findsOneWidget);
  });

  testWidgets('存在しないルートでエラーページが表示されること', (tester) async {
    // When: 存在しないルートへアクセス
    router.go('/nonexistent');
    await tester.pumpAndSettle();

    // Then: エラーページが表示されること
    expect(find.byType(ErrorPage), findsOneWidget);
  });
});
```

**推定テストケース数:** 10ケース
**推定工数:** 25分

---

### テスト2: リダイレクトガードテスト

**ファイル:** test/app/route_guard/nickname_guard_test.dart

**テストケース:**

```dart
group('NicknameGuard', () {
  test('ニックネーム未設定時に /posts へアクセスすると /nickname へリダイレクトされること', () {
    // Given: ニックネームが未設定
    final state = GoRouterState(
      uri: Uri.parse('/posts'),
      matchedLocation: '/posts',
    );

    // When: ガードを実行
    final redirectPath = nicknameGuard(context, state, ref);

    // Then: /nickname へリダイレクトされること
    expect(redirectPath, equals('/nickname'));
  });

  test('ニックネーム設定済みの場合はリダイレクトされないこと', () {
    // Given: ニックネームが設定済み
    ref.read(temporaryNicknameProvider.notifier).setNickname('テストユーザー');
    final state = GoRouterState(
      uri: Uri.parse('/posts'),
      matchedLocation: '/posts',
    );

    // When: ガードを実行
    final redirectPath = nicknameGuard(context, state, ref);

    // Then: リダイレクトされないこと
    expect(redirectPath, isNull);
  });

  test('/nickname 自体はリダイレクト対象外であること', () {
    // Given: ニックネームが未設定で /nickname にアクセス
    final state = GoRouterState(
      uri: Uri.parse('/nickname'),
      matchedLocation: '/nickname',
    );

    // When: ガードを実行
    final redirectPath = nicknameGuard(context, state, ref);

    // Then: リダイレクトされないこと
    expect(redirectPath, isNull);
  });
});
```

**推定テストケース数:** 8ケース
**推定工数:** 15分

---

### テスト3: エラーページテスト

**ファイル:** test/shared/presentation/pages/error_page_test.dart

**テストケース:**

```dart
group('ErrorPage', () {
  testWidgets('エラーメッセージが表示されること', (tester) async {
    // Given: エラーを含む状態
    const error = Exception('テストエラー');

    // When: ErrorPage を表示
    await tester.pumpWidget(
      MaterialApp(home: ErrorPage(error: error)),
    );

    // Then: エラーメッセージが表示されること
    expect(find.text('ページが見つかりません'), findsOneWidget);
  });

  testWidgets('ホームへ戻るボタンが表示されること', (tester) async {
    // When: ErrorPage を表示
    await tester.pumpWidget(
      MaterialApp(home: ErrorPage(error: null)),
    );

    // Then: ホームボタンが表示されること
    expect(find.text('ホームに戻る'), findsOneWidget);
  });

  testWidgets('ホームボタンをタップすると / へ遷移すること', (tester) async {
    // Given: ErrorPage を表示
    await tester.pumpWidget(
      MaterialApp.router(routerConfig: AppRouter.createRouter(ref)),
    );
    router.go('/nonexistent'); // エラーページへ
    await tester.pumpAndSettle();

    // When: ホームボタンをタップ
    await tester.tap(find.text('ホームに戻る'));
    await tester.pumpAndSettle();

    // Then: / へ遷移すること
    expect(router.routeInformationProvider.value.uri.path, equals('/nickname'));
  });
});
```

**推定テストケース数:** 5ケース
**推定工数:** 10分

---

### テスト実行コマンド

```bash
# すべてのテストを実行
fvm flutter test

# カバレッジ付きでテスト実行
fvm flutter test --coverage

# 特定のテストファイルを実行
fvm flutter test test/app/app_router/app_router_test.dart

# カバレッジレポートの生成（オプション）
genhtml coverage/lcov.info -o coverage/html
```

---

### モックの使用

**mockito を使用したモック:**

```dart
// test/app/app_router/app_router_test.dart
import 'package:mockito/mockito.dart';
import 'package:mockito/annotations.dart';

@GenerateMocks([WidgetRef])
void main() {
  late MockWidgetRef mockRef;

  setUp(() {
    mockRef = MockWidgetRef();
  });

  // テストケース...
}
```

---

## リスク分析と対策

### リスク1: Issue #8 への依存

**リスクレベル:** 🔴 High

**詳細:**
- Issue #8（プロジェクト構造組織化）が未完了
- lib/features/ ディレクトリが存在しない
- 各画面（NicknamePage等）が未実装

**影響:**
- 実際のページを使用できない
- プレースホルダーページで代替が必要

**対策:**
1. ✅ **短期対策**: プレースホルダーページを lib/app/widgets/pages/ に作成
2. ✅ **移行計画**: Issue #8 完了後、以下の手順で移行
   - lib/features/ に実際のページを作成
   - app_router.dart のインポートパスを変更
   - プレースホルダーページを削除

**移行の容易性:**
- インポートパスの変更のみで対応可能
- ルーティングロジックの変更は不要

---

### リスク2: ニックネーム管理の一時実装

**リスクレベル:** 🟡 Medium

**詳細:**
- shared_preferences が未導入（Issue #10で導入）
- 一時的にメモリ上でニックネームを管理
- アプリ再起動時にニックネームが消失

**影響:**
- 開発・テスト時にニックネームの再入力が必要
- 実運用には不適切

**対策:**
1. ✅ **一時対策**: メモリ上での管理を明確にドキュメント化
2. ✅ **移行計画**: Issue #10 で shared_preferences 対応版に置き換え
3. ✅ **テスト**: 一時実装でも動作するようテストを設計

**移行時の作業:**
```dart
// 変更前（一時実装）
@riverpod
class TemporaryNickname extends _$TemporaryNickname {
  @override
  String? build() => null; // メモリのみ
}

// 変更後（Issue #10）
@riverpod
class Nickname extends _$Nickname {
  @override
  Future<String?> build() async {
    final prefs = await SharedPreferences.getInstance();
    return prefs.getString('nickname'); // 永続化
  }
}
```

---

### リスク3: go_router_builder 未使用

**リスクレベル:** 🟢 Low

**詳細:**
- 現時点では基本的な GoRoute を使用
- go_router_builder によるコード生成は未使用
- 型安全性が若干劣る

**影響:**
- タイプセーフなナビゲーションが制限される
- ルート定義の変更時にコンパイルエラーで検出できない

**対策:**
1. ✅ **現在**: Routes クラスで定数管理により型安全性を部分的に確保
2. ✅ **将来**: Issue #8 完了後に go_router_builder へ移行を検討
3. ✅ **ドキュメント**: 移行手順を CLAUDE.md に記載

**移行時の作業:**
```dart
// 変更前（現在）
GoRoute(
  path: Routes.posts,
  builder: (context, state) => const PostsPage(),
)

// 変更後（go_router_builder）
@TypedGoRoute<PostsRoute>(path: '/posts')
class PostsRoute extends GoRouteData {
  @override
  Widget build(BuildContext context, GoRouterState state) {
    return const PostsPage();
  }
}
```

---

### リスク4: Riverpod Provider のスコープ問題

**リスクレベル:** 🟡 Medium

**詳細:**
- GoRouter の初期化時に WidgetRef へのアクセスが必要
- main.dart での createRouter() 呼び出し時に ref が取得困難

**影響:**
- リダイレクトガードで Provider にアクセスできない可能性

**対策:**
1. ✅ **解決策**: ConsumerWidget を使用して ref を取得
2. ✅ **実装例**:

```dart
// main.dart
class MainApp extends ConsumerWidget {
  const MainApp({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return MaterialApp.router(
      title: 'Flutter Hackathon Thema',
      routerConfig: AppRouter.createRouter(ref),
    );
  }
}
```

3. ✅ **代替案**: go_router の refreshListenable で Provider を監視

```dart
GoRouter(
  refreshListenable: GoRouterRefreshStream(
    ref.watch(temporaryNicknameProvider.notifier).stream,
  ),
  // ...
)
```

---

### リスク5: テスト実装の複雑さ

**リスクレベル:** 🟡 Medium

**詳細:**
- ルーティングテストは Widget テストが必要
- GoRouter のテストは非同期処理が多い
- mockito によるモックが複雑

**影響:**
- テスト実装に想定以上の時間がかかる可能性
- テストの保守性が低下する可能性

**対策:**
1. ✅ **ヘルパー関数**: テスト用のヘルパー関数を作成
2. ✅ **シンプル化**: テストケースを最小限に絞る
3. ✅ **ドキュメント**: テストパターンをドキュメント化

**テストヘルパー例:**
```dart
// test/helpers/router_test_helper.dart
Widget createTestApp(GoRouter router) {
  return ProviderScope(
    child: MaterialApp.router(routerConfig: router),
  );
}

Future<void> navigateAndSettle(WidgetTester tester, String path) async {
  final router = GoRouter.of(tester.element(find.byType(MaterialApp)));
  router.go(path);
  await tester.pumpAndSettle();
}
```

---

### リスク6: プレースホルダーページの削除忘れ

**リスクレベル:** 🟢 Low

**詳細:**
- Issue #8 完了後、プレースホルダーページを削除する必要
- 削除忘れによるコードの肥大化

**影響:**
- 不要なコードが残る
- 保守性の低下

**対策:**
1. ✅ **TODO コメント**: プレースホルダーファイルに TODO を追加
2. ✅ **Issue #8 でタスク化**: Issue #8 の実装タスクに削除を含める
3. ✅ **チェックリスト**: Issue #8 の完了チェックリストに追加

```dart
// lib/app/widgets/pages/placeholder_nickname_page.dart
// TODO(#8): Issue #8 完了後、このファイルを削除して
// lib/features/nickname/presentation/pages/nickname_page.dart に置き換える
```

---

## 技術スタック互換性

### 依存パッケージの確認

**現在の pubspec.yaml:**

```yaml
dependencies:
  flutter:
    sdk: flutter
  hooks_riverpod: ^3.0.3          # ✅ 使用
  go_router: ^16.2.4               # ✅ 使用
  freezed_annotation: ^3.1.0       # ❌ 未使用（このIssue内）
  json_annotation: ^4.9.0          # ❌ 未使用

dev_dependencies:
  flutter_test:
    sdk: flutter
  riverpod_annotation: ^3.0.3      # ✅ 使用（Provider）
  riverpod_generator: ^3.0.3       # ✅ 使用（コード生成）
  build_runner: ^2.4.13            # ✅ 使用（コード生成）
  go_router_builder: ^4.1.0        # ❌ 未使用（将来的に使用）
  freezed: ^3.2.3                  # ❌ 未使用（このIssue内）
  mockito: ^5.4.4                  # ✅ 使用（テスト）
  very_good_analysis: ^10.0.0      # ✅ 使用（静的解析）
  riverpod_lint: ^3.0.3            # ✅ 使用（Lint）
```

### 互換性確認

| パッケージ | バージョン | 互換性 | 備考 |
|----------|----------|-------|------|
| hooks_riverpod | ^3.0.3 | ✅ OK | Riverpod 3.x 系を使用 |
| riverpod_annotation | ^3.0.3 | ✅ OK | @riverpod アノテーション対応 |
| riverpod_generator | ^3.0.3 | ✅ OK | コード生成対応 |
| go_router | ^16.2.4 | ✅ OK | 最新の go_router 16.x |
| go_router_builder | ^4.1.0 | ⚠️ 未使用 | 将来的に使用予定 |
| mockito | ^5.4.4 | ✅ OK | テストモック対応 |
| very_good_analysis | ^10.0.0 | ✅ OK | 静的解析ルール |

### Dart SDK 互換性

```yaml
environment:
  sdk: '>=3.9.0 <4.0.0'  # ✅ OK
```

- Riverpod 3.x は Dart 3.0+ が必要
- go_router 16.x は Dart 3.0+ が必要
- すべてのパッケージが Dart 3.9.0 と互換性あり

### Flutter SDK 互換性

```yaml
environment:
  sdk: '>=3.24.0'  # ✅ OK
```

- go_router 16.x は Flutter 3.24.0+ が必要
- hooks_riverpod 3.x は Flutter 3.10+ が必要
- すべての要件を満たしている

---

## 実装チェックリスト

### Phase 1: 環境準備

- [ ] ブランチ作成: `git checkout -b feature/routing-configuration`
- [ ] 最新の main ブランチから分岐していることを確認
- [ ] FVM が正しく設定されていることを確認: `fvm flutter --version`

### Phase 2: ディレクトリ作成

- [ ] `mkdir -p lib/app/route_guard`
- [ ] `mkdir -p lib/app/app_di`
- [ ] `mkdir -p lib/app/widgets/pages`
- [ ] `mkdir -p lib/shared/presentation/pages`
- [ ] `mkdir -p test/app/app_router`
- [ ] `mkdir -p test/app/route_guard`
- [ ] `mkdir -p test/shared/presentation/pages`

### Phase 3: コード実装

#### App Layer - Routing

- [ ] lib/app/app_router/routes.dart を変更
  - [ ] Routes.nickname 定数を追加
  - [ ] Routes.posts 定数を追加
  - [ ] Routes.create 定数を追加
  - [ ] Routes.preview 定数を追加
  - [ ] HomeRoute クラスを削除
  - [ ] ドキュメントコメントを追加

- [ ] lib/app/app_router/app_router.dart を変更
  - [ ] createRouter メソッドのシグネチャに WidgetRef を追加
  - [ ] initialLocation を Routes.nickname に変更
  - [ ] /nickname ルートを追加
  - [ ] /posts ルートを追加
  - [ ] /create ルートを追加
  - [ ] /preview ルートを追加
  - [ ] redirect パラメータを追加
  - [ ] errorBuilder を ErrorPage に置き換え

- [ ] lib/app/route_guard/nickname_guard.dart を作成
  - [ ] nicknameGuard 関数を実装
  - [ ] ニックネーム未設定時のリダイレクトロジック
  - [ ] ニックネームページ自体の除外ロジック

#### App Layer - Dependency Injection

- [ ] lib/app/app_di/nickname_provider.dart を作成
  - [ ] @riverpod アノテーションを使用
  - [ ] TemporaryNickname クラスを実装
  - [ ] setNickname メソッドを実装
  - [ ] clearNickname メソッドを実装

#### App Layer - Placeholder Pages

- [ ] lib/app/widgets/pages/placeholder_nickname_page.dart を作成
  - [ ] PlaceholderNicknamePage ウィジェットを実装
  - [ ] タイトルとメッセージを表示
  - [ ] ナビゲーションボタンを追加
  - [ ] TODO コメントを追加

- [ ] lib/app/widgets/pages/placeholder_posts_page.dart を作成
  - [ ] PlaceholderPostsPage ウィジェットを実装
  - [ ] タイトルとメッセージを表示
  - [ ] /create へのナビゲーションボタンを追加

- [ ] lib/app/widgets/pages/placeholder_create_post_page.dart を作成
  - [ ] PlaceholderCreatePostPage ウィジェットを実装
  - [ ] タイトルとメッセージを表示
  - [ ] /posts へのナビゲーションボタンを追加

- [ ] lib/app/widgets/pages/placeholder_preview_page.dart を作成
  - [ ] PlaceholderPreviewPage ウィジェットを実装
  - [ ] タイトルとメッセージを表示
  - [ ] /posts へのナビゲーションボタンを追加

#### Shared Layer - Error Handling

- [ ] lib/shared/presentation/pages/error_page.dart を作成
  - [ ] ErrorPage ウィジェットを実装
  - [ ] エラーアイコンを表示
  - [ ] エラーメッセージを表示
  - [ ] ホームへ戻るボタンを実装
  - [ ] AppColors, AppTextStyles を使用

#### Main App

- [ ] lib/main.dart を変更
  - [ ] MainApp を StatelessWidget から ConsumerWidget に変更
  - [ ] MaterialApp を MaterialApp.router に変更
  - [ ] routerConfig に AppRouter.createRouter(ref) を設定
  - [ ] title を 'Flutter Hackathon Thema' に変更

### Phase 4: コード生成

- [ ] `fvm flutter pub run build_runner build --delete-conflicting-outputs`
- [ ] nickname_provider.g.dart が生成されたことを確認
- [ ] ビルドエラーがないことを確認

### Phase 5: 静的解析とフォーマット

- [ ] `dart format --set-exit-if-changed .`
- [ ] すべてのファイルがフォーマットされたことを確認
- [ ] `fvm flutter analyze`
- [ ] 静的解析エラーが0件であることを確認
- [ ] riverpod_lint の警告がないことを確認

### Phase 6: 動作確認

- [ ] `fvm flutter run` でアプリを起動
- [ ] /nickname ページが初期表示されることを確認
- [ ] ニックネームを入力せずに /posts へ遷移しようとすると /nickname へリダイレクトされることを確認
- [ ] ニックネームを入力後、/posts へ遷移できることを確認
- [ ] /create, /preview へのナビゲーションが機能することを確認
- [ ] 存在しないルート（例: /nonexistent）でエラーページが表示されることを確認
- [ ] エラーページの「ホームに戻る」ボタンが機能することを確認

### Phase 7: テスト実装

- [ ] test/app/app_router/app_router_test.dart を作成
  - [ ] ルート定義のテストを実装
  - [ ] 初期ルートのテストを実装
  - [ ] エラービルダーのテストを実装

- [ ] test/app/route_guard/nickname_guard_test.dart を作成
  - [ ] リダイレクトロジックのテストを実装
  - [ ] ニックネーム未設定時のテストを実装
  - [ ] ニックネーム設定済み時のテストを実装

- [ ] test/shared/presentation/pages/error_page_test.dart を作成
  - [ ] エラーページ表示のテストを実装
  - [ ] ホームボタンのテストを実装

### Phase 8: テスト実行

- [ ] `fvm flutter test`
- [ ] すべてのテストがパスすることを確認
- [ ] `fvm flutter test --coverage`
- [ ] テストカバレッジが目標値（ユニット80%、Widget60%）を達成していることを確認

### Phase 9: ドキュメント更新

- [ ] CLAUDE.md の Essential Commands にルーティング関連コマンドを追加（必要に応じて）
- [ ] README.md の更新（必要に応じて）

### Phase 10: コミットとプッシュ

- [ ] 変更をステージング: `git add .`
- [ ] コミット: `git commit -m "feat(routing): Implement go_router configuration with nickname guard"`
- [ ] プッシュ: `git push origin feature/routing-configuration`

### Phase 11: PR作成（オプション）

- [ ] GitHub で Pull Request を作成
- [ ] タイトル: `feat(routing): Issue #9 - Routing Configuration (go_router Integration)`
- [ ] 説明文に以下を含める:
  - [ ] 実装内容の概要
  - [ ] テスト結果
  - [ ] スクリーンショット（ページ遷移の様子）
  - [ ] 既知の制限事項（プレースホルダーページの使用）

---

## 完了条件

### 必須条件

1. ✅ すべてのファイルが正しく作成・変更されている
2. ✅ `fvm flutter analyze` でエラー0件
3. ✅ `fvm flutter test` ですべてのテストがパス
4. ✅ テストカバレッジが目標値を達成
5. ✅ アプリが正常に起動し、ルーティングが機能する
6. ✅ リダイレクトガードが正しく動作する
7. ✅ エラーページが正しく表示される

### 推奨条件

1. ✅ コードがスタイルガイドに準拠している
2. ✅ ドキュメントコメントが適切に記載されている
3. ✅ TODO コメントが適切に追加されている
4. ✅ PR が作成されている（チーム開発の場合）

---

## 次のステップ

### Issue #8 完了後の移行作業

1. **プレースホルダーページの置き換え**
   - lib/features/nickname/presentation/pages/nickname_page.dart を作成
   - lib/features/posts/presentation/pages/posts_page.dart を作成
   - lib/features/post_creation/presentation/pages/create_post_page.dart を作成
   - lib/features/post_creation/presentation/pages/preview_page.dart を作成

2. **app_router.dart のインポート更新**
   ```dart
   // 変更前
   import 'package:flutterhackthema/app/widgets/pages/placeholder_nickname_page.dart';

   // 変更後
   import 'package:flutterhackthema/features/nickname/presentation/pages/nickname_page.dart';
   ```

3. **プレースホルダーページの削除**
   ```bash
   rm -rf lib/app/widgets/pages/placeholder_*
   ```

4. **テストの更新**
   - プレースホルダーページを参照しているテストを更新
   - 実際のページの動作をテスト

### Issue #10 完了後の移行作業

1. **ニックネーム Provider の置き換え**
   - lib/app/app_di/nickname_provider.dart を削除
   - lib/features/nickname/presentation/providers/nickname_provider.dart を使用

2. **リダイレクトガードの更新**
   ```dart
   // 変更前
   final nickname = ref.read(temporaryNicknameProvider);

   // 変更後
   final nickname = ref.read(nicknameProvider);
   ```

3. **テストの更新**
   - shared_preferences のモックを使用したテストに更新

---

## まとめ

### 実装内容の要約

本プランでは、Issue #9（ルーティング設定）を実装するための詳細な戦略を定義しました。主要な実装内容:

1. **ルーティング基盤の構築**
   - go_router 16.x を使用したグローバルルーティング
   - 4つのルート定義（/nickname, /posts, /create, /preview）

2. **リダイレクトガードの実装**
   - ニックネーム未設定時の自動リダイレクト
   - 一時的な Provider によるニックネーム管理

3. **プレースホルダーページの作成**
   - Issue #8 完了までの暫定対応
   - lib/app/widgets/pages/ に配置

4. **エラーハンドリング**
   - 専用エラーページの作成
   - 404エラーの適切な処理

5. **包括的なテスト**
   - ルーティングテスト
   - リダイレクトガードテスト
   - エラーページテスト

### アーキテクチャ遵守

- ✅ 三層アーキテクチャに準拠
- ✅ App → Feature → Shared の依存関係を遵守
- ✅ Feature 間の直接依存なし
- ✅ Shared 層の完全独立性を維持

### 品質保証

- ✅ テストカバレッジ目標: ユニット80%、Widget60%
- ✅ 静的解析エラー0件
- ✅ コーディング規約遵守
- ✅ ドキュメントコメント完備

### リスク管理

- ✅ Issue #8 への依存に対する対策
- ✅ 一時実装の明確化と移行計画
- ✅ 技術的負債の最小化

---

**次のアクション:**
1. このプランの承認を得る
2. `feature/routing-configuration` ブランチを作成
3. 実装チェックリストに従って実装を開始
4. 各フェーズ完了後にレビューを実施

**推定工数:** 2-3時間
**ブロック対象:** Issue #10, #12, #13, #15
**次のフェーズ:** IMPLEMENT

---

*このプランは、調査レポート `investigate_20251129_140127.md` に基づいて作成されました。*
