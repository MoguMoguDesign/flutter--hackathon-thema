rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // デフォルトルール: 認証済みユーザーのみ読み書き可能
    // 本番環境では各パスごとに適切なルールを設定すること
    match /{allPaths=**} {
      // 認証済みユーザーのみアクセス可能
      allow read, write: if request.auth != null;
    }

    // ユーザーファイルの例（将来的に追加予定）
    // match /users/{userId}/{allPaths=**} {
    //   // 自分のファイルのみ読み書き可能
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }
  }
}
